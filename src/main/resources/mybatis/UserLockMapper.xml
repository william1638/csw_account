<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.std.account.dao.IUserLockDAO">
	<resultMap id="userLock_DB_Result" type="userLock">
		<result column="user_id" property="userId" />
		<result column="lock_direction" property="lockDirection" />		
		<result column="remark" property="remark" />
		<result column="create_datetime" property="createDatetime" />			
	</resultMap>
	<sql id="where_condition">
		<trim prefix="WHERE" prefixOverrides="AND | OR">			
			<if test="userId != null and userId != '' ">
				AND t.user_id = #{userId}
			</if>	
			<if test="lockDirection != null and lockDirection != '' ">
				AND t.lock_direction = #{lockDirection}
			</if>		
			<if test="createDatetimeStart != null and createDatetimeStart != '' ">
				<![CDATA[AND t.create_datetime >= #{createDatetimeStart}]]>
			</if>
			<if test="createDatetimeEnd != null and createDatetimeEnd != '' ">
				<![CDATA[AND t.create_datetime <= #{createDatetimeEnd}]]>
			</if>		
		</trim>
	</sql>
	<select id="select_userLock" parameterType="userLock" resultMap="userLock_DB_Result">
		SELECT 
		user_id
		,lock_direction
		,remark	
		,create_datetime			
		FROM
		tstd_user_lock t
		<include refid="where_condition"/>
		ORDER BY t.create_datetime DESC	
	</select>

	<select id="select_userLock_count" parameterType="userLock" resultType="java.lang.Long">
		SELECT count(1) FROM tstd_user_lock t
		<include refid="where_condition"/>
	</select>

	<insert id="insert_userLock" parameterType="userLock">
		INSERT INTO tstd_user_lock(
		user_id
		,lock_direction
		,remark	
		,create_datetime			
		)
		VALUES(
		#{userId}
		,#{lockDirection}
		,#{remark}	
		,#{createDatetime}		
		)
	</insert>
	
</mapper>
